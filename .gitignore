# Unity generated
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
MemoryCaptures/
*.csproj
*.sln
*.user
*.pidb
*.booproj
*.svd
*.tmp
.vs/
.idea/
crashlytics-build.properties
*.log

# Batch test / screenshot artifacts in project root (not the GameScreenshots folder)
/*.xml
/shot_*.png
/menu_*.png
/screenshot_*.png
/clean*.png
/setup.log

# Asset pipeline staging (raw downloads before import)
_AssetDownloads/

# Abandoned 2D web prototype — direction is locked to Unity 3D native.
# Kept on disk for reference, but not part of the game repo.
webproto/
boardwalk-rush.zip
Tools/serve.js

# Unity crash-recovery scene dupes (not real work)
Assets/_Recovery/
Assets/_Recovery.meta

# Editor / machine-specific
.claude/

# Editor / IDE local config (machine-specific, not part of the game)
.vscode/
.continue/
*.slnx
